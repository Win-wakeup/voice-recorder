const fs = require('fs'); let code = fs.readFileSync('main.js', 'utf8'); code = code.replace(/const searchInput = document.getElementById\\('searchInput'\\);/g, 'const getEl = (id) => document.getElementById(id);\\nlet searchInput, searchBtn, loadingPanel, initialPanel, noResultsPanel, hotelList, suggestionsBox, distanceSelect, locateBtn;'); code = code.replace(/const searchBtn = .*?\\n.*?\\n.*?\\n.*?\\n.*?\\n.*?\\n/g, ''); code = code.replace(/function init\\(\\) \\{/, 'function init() {\\n  searchInput = getEl(\\'searchInput\\');\\n  searchBtn = getEl(\\'searchBtn\\');\\n  loadingPanel = getEl(\\'loading\\');\\n  initialPanel = getEl(\\'initial\\');\\n  noResultsPanel = getEl(\\'noResults\\');\\n  hotelList = getEl(\\'hotelList\\');\\n  suggestionsBox = getEl(\\'suggestionsBox\\');\\n  distanceSelect = getEl(\\'distanceSelect\\');\\n  locateBtn = getEl(\\'locateBtn\\');\\n\\n  if(window.appInitialized) return;\\n  window.appInitialized = true;\\n'); fs.writeFileSync('main.js', code);
